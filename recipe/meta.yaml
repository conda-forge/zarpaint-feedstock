{% set python_min = "3.11" %}
{% set name = "zarpaint" %}
{% set version = "0.4.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/zarpaint-{{ version }}.tar.gz
  sha256: 3b322a62d80b199170b57c19cdef3665f0de80f02d4192d3e132ea65bb03b12e

build:
  entry_points:
    - zarpaint = zarpaint._main:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python {{ python_min }}
    - setuptools
    - setuptools-scm
  run:
    - magicgui
    - napari >=0.4.19
    - numpy
    - python >={{ python_min }}
    - pyyaml
    - qtpy
    - scikit-image >=0.21
    - scipy
    - toolz
    - zarr >=2.11

test:
  imports:
    - zarpaint
  commands:
    - pip list
    - pip check
  requires:
    - pip
    - pyqt
    - python {{ python_min }}

about:
  home: https://pypi.org/project/zarpaint
  summary: Paint segmentations directly to on-disk/remote zarr arrays
  license: BSD-3-Clause
  license_file: LICENSE
  dev_url: https://github.com/jni/zarpaint

extra:
  recipe-maintainers:
    - goanpeca
    - jaimergp
    - jni
